# REL+ v2 source audit

Baseline identity: GitHub `Dejavu6868/REL-Expansion`, reviewed commit `2895c42`, directory `RELPlus0818`.

Audited executable sources on the server:

- `/home/zhuzhaoziao/RELPlus/CMX-REL/third_party/rel_original/rel.py`: `getREL()` and stack `[angle, HA, RD]`.
- `/home/zhuzhaoziao/RELPlus/REL-SF4PASS-reference/utils/rgbd_util.py`: `getPointCloudFromZ()` and `computeNormalsSquareSupport()`.
- `/data/bxh_copy/Pano_MA_Seg/utils/hha_util.py`: `filterItChopOff()`, `invertIt()`, `mutiplyIt()`, `getRMatrix()` and `rotatePC()`.
- `/home/zhuzhaoziao/rel_exp/cmx_rel+/dataloader/dataloader.py`: CMX interpolation/order and shared normalization contract.
- `/home/zhuzhaoziao/rel_exp/cmx_rel+/utils/transforms.py`: `normalize()` and crop/centred-pad behavior.

Confirmed call mapping:

- Stanford `raw / 512`, invalid `{0,65535}`, JSON half-pixel K and explicit W2C are unchanged from v1.
- Perspective normal helper receives metric depth and internally multiplies by 100; the caller does not pre-scale it.
- Gravity alignment remains `getRMatrix(target, source)` then `rotatePC(..., R.T)`, except that anti-parallel input is now an explicit typed failure.
- Only aligned encoding points are multiplied by 100 before ReD/height/LOA.
- ReD full-image min/max, EGVIA full-image P1/P99 and `~is_horizontal`, LOA degrees and uint8 truncation are preserved.

Offline `tests/fixtures/source_golden.npz` was generated by `tools/create_source_golden_fixture.py` from the functions above. Offline tests compare rotation, rotated points, normals, plane offset and all encoded bytes. The `live_source` test repeats these comparisons when both audited roots are supplied and skips explicitly otherwise.
